<template>
  <section class="fm-grid-shell fm-ui-grid-shell" :class="{ 'fm-grid-shell--loading': loading, 'fm-ui-grid-shell--loading': loading }">
    <header v-if="title || $slots.title || $slots.actions" class="fm-grid-title fm-ui-grid-title">
      <slot name="title">
        <span>{{ title }}</span>
      </slot>
      <slot name="actions" />
    </header>

    <slot />

    <FmTypingLoader
      v-if="loading"
      overlay
      variant="grid"
      :title="loadingTitle"
      :message="loadingMessage"
    />
  </section>
</template>

<script setup>
/**
 * Contenedor global de grillas Field Manager.
 *
 * COMO USAR EN OTRAS PANTALLAS:
 *
 * <FmGridShell :loading="loading" loading-message="Cargando datos">
 *   <DataTable :value="rows" />
 * </FmGridShell>
 *
 * El loader usa automaticamente el tipito tipeando global.
 */
defineProps({
  title: { type: String, default: '' },
  loading: { type: Boolean, default: false },
  loadingTitle: { type: String, default: 'Cargando' },
  loadingMessage: { type: String, default: 'Cargando grilla' }
})
</script>
